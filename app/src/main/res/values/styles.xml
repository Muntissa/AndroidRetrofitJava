<resources>

    <style name="Widget.AndroidJava.AppWidget.Container" parent="android:Widget">
        <item name="android:id">@android:id/background</item>
        <item name="android:background">?android:attr/colorBackground</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Spinner Item Layout -->
        <item name="android:spinnerItemStyle">@style/Widget.AppCompat.Spinner</item>
    </style>
    <style name="YourCustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:popupBackground">@color/colorPrimary</item>
        <item name="android:dropDownSelector">@color/colorAccent</item>
        <!-- Другие параметры стиля, если необходимо -->
    </style>

    <style name="Widget.AndroidJava.AppWidget.InnerView" parent="android:Widget">
        <item name="android:background">?android:attr/colorBackground</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>
</resources>